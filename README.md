# TRAFFIC-LIGHT-CONTROLLER-FSM
Implemented a Traffic Light Controller using Verilog HDL and Finite State Machine (FSM) design. The controller cycles through RED, GREEN, and YELLOW states and drives the corresponding outputs. Verified functionality through RTL simulation and waveform analysis.
